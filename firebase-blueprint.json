{
  "entities": {
    "userWorkspaces": {
      "title": "User Workspace Data",
      "description": "User's persistent workspace preferences, notes, timetable, tasks, courses, and countdowns",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The unique identifier or account ID of the user"
        },
        "data": {
          "type": "object",
          "description": "The complete workspace data JSON structure"
        },
        "updatedAt": {
          "type": "string",
          "description": "ISO timestamp of last workspace sync"
        }
      },
      "required": ["userId", "data"]
    },
    "contactMessages": {
      "title": "Contact Messages",
      "description": "Contact form submissions directed to bibiworkspace@hotmail.com",
      "type": "object",
      "properties": {
        "to": {
          "type": "string"
        },
        "senderName": {
          "type": "string"
        },
        "senderEmail": {
          "type": "string"
        },
        "subject": {
          "type": "string"
        },
        "message": {
          "type": "string"
        },
        "createdAt": {
          "type": "string"
        }
      },
      "required": ["to", "senderName", "senderEmail", "message"]
    }
  }
}
